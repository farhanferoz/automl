"""Baseline model wrappers for benchmarking.

Thin wrappers around established ML models, following the BaseModel interface
so they can participate in the same evaluation pipeline as the research models.
"""
